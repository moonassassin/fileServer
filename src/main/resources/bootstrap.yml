spring:
  cloud:
    config:
      discovery:
        enabled: true
        serviceId: configServer
      #uri: @config.uri@
      name: @config.name@
      profile: @config.profile@
      
eureka:
  client:
    serviceUrl:
      defaultZone: @service.url@
